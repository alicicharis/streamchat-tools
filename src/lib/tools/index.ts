import { weather } from '@/lib/tools/weather';
import { calculator } from '@/lib/tools/calculator';
import { currency } from '@/lib/tools/currency';

export const tools = { weather, calculator, currency };
